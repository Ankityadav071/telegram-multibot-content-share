V9.8.0 STEP 2 — Storage Evolution

Implemented:
- /status now reports an active replacement operation before pending upload state.
- Storage Health scans the full catalogue in pages instead of a hard 10,000-video cap.
- Health now shows Primary/Backup coverage percentages and fully-mirrored percentage.
- Health remains read-only; Telegram reachability probing stays explicit under Repair Specific Video.
- Added direct Upload / Replace navigation from Storage Health.
- py_compile passed for all Python modules.
